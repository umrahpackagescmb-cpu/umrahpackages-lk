/**
 * PLACEHOLDER — regenerate once a real Supabase project exists:
 *
 *   npx supabase gen types typescript --project-id <ref> > src/lib/supabase/database.types.ts
 *
 * Typed as `any` in the meantime so src/lib/supabase/{client,server}.ts
 * compile before the schema in /supabase/migrations has a live project to
 * generate against. Every table/enum in that schema will appear here once
 * regenerated — see /supabase/migrations/0001_init_schema.sql for the
 * source of truth today.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
